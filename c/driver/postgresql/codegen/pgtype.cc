// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

// !! DO NOT EDIT!!
// Auto-generated by pgtype.py.

#include <nanoarrow/nanoarrow.hpp>
#include "postgresql/postgres_type.h"

struct ArrowError;

namespace adbcpq {

ArrowErrorCode InitializeTypeResolver(PostgresTypeResolver& resolver, ArrowError* error) {
  int rc = 0;
  PostgresTypeResolver::Item type_item;
  type_item.class_oid = 0;
  type_item.base_oid = 0;

  type_item.oid = 16;
  type_item.typname = "bool";
  type_item.typreceive = "boolrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1000;
  type_item.typname = "_bool";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 16;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 17;
  type_item.typname = "bytea";
  type_item.typreceive = "bytearecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1001;
  type_item.typname = "_bytea";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 17;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 18;
  type_item.typname = "char";
  type_item.typreceive = "charrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1002;
  type_item.typname = "_char";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 18;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 19;
  type_item.typname = "name";
  type_item.typreceive = "namerecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1003;
  type_item.typname = "_name";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 19;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 20;
  type_item.typname = "int8";
  type_item.typreceive = "int8recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1016;
  type_item.typname = "_int8";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 20;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 21;
  type_item.typname = "int2";
  type_item.typreceive = "int2recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1005;
  type_item.typname = "_int2";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 21;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 22;
  type_item.typname = "int2vector";
  type_item.typreceive = "int2vectorrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1006;
  type_item.typname = "_int2vector";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 22;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 23;
  type_item.typname = "int4";
  type_item.typreceive = "int4recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1007;
  type_item.typname = "_int4";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 23;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 24;
  type_item.typname = "regproc";
  type_item.typreceive = "regprocrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1008;
  type_item.typname = "_regproc";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 24;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 25;
  type_item.typname = "text";
  type_item.typreceive = "textrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1009;
  type_item.typname = "_text";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 25;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 26;
  type_item.typname = "oid";
  type_item.typreceive = "oidrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1028;
  type_item.typname = "_oid";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 26;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 27;
  type_item.typname = "tid";
  type_item.typreceive = "tidrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1010;
  type_item.typname = "_tid";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 27;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 28;
  type_item.typname = "xid";
  type_item.typreceive = "xidrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1011;
  type_item.typname = "_xid";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 28;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 29;
  type_item.typname = "cid";
  type_item.typreceive = "cidrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1012;
  type_item.typname = "_cid";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 29;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 30;
  type_item.typname = "oidvector";
  type_item.typreceive = "oidvectorrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1013;
  type_item.typname = "_oidvector";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 30;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 114;
  type_item.typname = "json";
  type_item.typreceive = "json_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 199;
  type_item.typname = "_json";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 114;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 142;
  type_item.typname = "xml";
  type_item.typreceive = "xml_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 143;
  type_item.typname = "_xml";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 142;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 194;
  type_item.typname = "pg_node_tree";
  type_item.typreceive = "pg_node_tree_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3361;
  type_item.typname = "pg_ndistinct";
  type_item.typreceive = "pg_ndistinct_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3402;
  type_item.typname = "pg_dependencies";
  type_item.typreceive = "pg_dependencies_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 5017;
  type_item.typname = "pg_mcv_list";
  type_item.typreceive = "pg_mcv_list_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 32;
  type_item.typname = "pg_ddl_command";
  type_item.typreceive = "pg_ddl_command_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 5069;
  type_item.typname = "xid8";
  type_item.typreceive = "xid8recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 271;
  type_item.typname = "_xid8";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 5069;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 600;
  type_item.typname = "point";
  type_item.typreceive = "point_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1017;
  type_item.typname = "_point";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 600;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 601;
  type_item.typname = "lseg";
  type_item.typreceive = "lseg_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1018;
  type_item.typname = "_lseg";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 601;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 602;
  type_item.typname = "path";
  type_item.typreceive = "path_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1019;
  type_item.typname = "_path";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 602;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 603;
  type_item.typname = "box";
  type_item.typreceive = "box_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1020;
  type_item.typname = "_box";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 603;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 604;
  type_item.typname = "polygon";
  type_item.typreceive = "poly_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1027;
  type_item.typname = "_polygon";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 604;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 628;
  type_item.typname = "line";
  type_item.typreceive = "line_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 629;
  type_item.typname = "_line";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 628;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 700;
  type_item.typname = "float4";
  type_item.typreceive = "float4recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1021;
  type_item.typname = "_float4";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 700;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 701;
  type_item.typname = "float8";
  type_item.typreceive = "float8recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1022;
  type_item.typname = "_float8";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 701;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 705;
  type_item.typname = "unknown";
  type_item.typreceive = "unknownrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 718;
  type_item.typname = "circle";
  type_item.typreceive = "circle_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 719;
  type_item.typname = "_circle";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 718;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 790;
  type_item.typname = "money";
  type_item.typreceive = "cash_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 791;
  type_item.typname = "_money";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 790;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 829;
  type_item.typname = "macaddr";
  type_item.typreceive = "macaddr_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1040;
  type_item.typname = "_macaddr";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 829;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 869;
  type_item.typname = "inet";
  type_item.typreceive = "inet_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1041;
  type_item.typname = "_inet";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 869;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 650;
  type_item.typname = "cidr";
  type_item.typreceive = "cidr_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 651;
  type_item.typname = "_cidr";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 650;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 774;
  type_item.typname = "macaddr8";
  type_item.typreceive = "macaddr8_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 775;
  type_item.typname = "_macaddr8";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 774;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1042;
  type_item.typname = "bpchar";
  type_item.typreceive = "bpcharrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1014;
  type_item.typname = "_bpchar";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1042;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1043;
  type_item.typname = "varchar";
  type_item.typreceive = "varcharrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1015;
  type_item.typname = "_varchar";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1043;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1082;
  type_item.typname = "date";
  type_item.typreceive = "date_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1182;
  type_item.typname = "_date";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1082;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1083;
  type_item.typname = "time";
  type_item.typreceive = "time_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1183;
  type_item.typname = "_time";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1083;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1114;
  type_item.typname = "timestamp";
  type_item.typreceive = "timestamp_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1115;
  type_item.typname = "_timestamp";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1114;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1184;
  type_item.typname = "timestamptz";
  type_item.typreceive = "timestamptz_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1185;
  type_item.typname = "_timestamptz";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1184;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1186;
  type_item.typname = "interval";
  type_item.typreceive = "interval_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1187;
  type_item.typname = "_interval";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1186;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1266;
  type_item.typname = "timetz";
  type_item.typreceive = "timetz_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1270;
  type_item.typname = "_timetz";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1266;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1560;
  type_item.typname = "bit";
  type_item.typreceive = "bit_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1561;
  type_item.typname = "_bit";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1560;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1562;
  type_item.typname = "varbit";
  type_item.typreceive = "varbit_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1563;
  type_item.typname = "_varbit";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1562;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1700;
  type_item.typname = "numeric";
  type_item.typreceive = "numeric_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1231;
  type_item.typname = "_numeric";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1700;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 1790;
  type_item.typname = "refcursor";
  type_item.typreceive = "textrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2201;
  type_item.typname = "_refcursor";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 1790;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2202;
  type_item.typname = "regprocedure";
  type_item.typreceive = "regprocedurerecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2207;
  type_item.typname = "_regprocedure";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2202;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2203;
  type_item.typname = "regoper";
  type_item.typreceive = "regoperrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2208;
  type_item.typname = "_regoper";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2203;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2204;
  type_item.typname = "regoperator";
  type_item.typreceive = "regoperatorrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2209;
  type_item.typname = "_regoperator";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2204;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2205;
  type_item.typname = "regclass";
  type_item.typreceive = "regclassrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2210;
  type_item.typname = "_regclass";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2205;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4191;
  type_item.typname = "regcollation";
  type_item.typreceive = "regcollationrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 4192;
  type_item.typname = "_regcollation";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4191;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2206;
  type_item.typname = "regtype";
  type_item.typreceive = "regtyperecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2211;
  type_item.typname = "_regtype";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2206;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4096;
  type_item.typname = "regrole";
  type_item.typreceive = "regrolerecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 4097;
  type_item.typname = "_regrole";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4096;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4089;
  type_item.typname = "regnamespace";
  type_item.typreceive = "regnamespacerecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 4090;
  type_item.typname = "_regnamespace";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4089;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 6490;
  type_item.typname = "regdatabase";
  type_item.typreceive = "regdatabaserecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6491;
  type_item.typname = "_regdatabase";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 6490;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2950;
  type_item.typname = "uuid";
  type_item.typreceive = "uuid_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2951;
  type_item.typname = "_uuid";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2950;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3220;
  type_item.typname = "pg_lsn";
  type_item.typreceive = "pg_lsn_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3221;
  type_item.typname = "_pg_lsn";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3220;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3614;
  type_item.typname = "tsvector";
  type_item.typreceive = "tsvectorrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3643;
  type_item.typname = "_tsvector";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3614;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3615;
  type_item.typname = "tsquery";
  type_item.typreceive = "tsqueryrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3645;
  type_item.typname = "_tsquery";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3615;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3734;
  type_item.typname = "regconfig";
  type_item.typreceive = "regconfigrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3735;
  type_item.typname = "_regconfig";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3734;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3769;
  type_item.typname = "regdictionary";
  type_item.typreceive = "regdictionaryrecv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3770;
  type_item.typname = "_regdictionary";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3769;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 3802;
  type_item.typname = "jsonb";
  type_item.typreceive = "jsonb_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 3807;
  type_item.typname = "_jsonb";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 3802;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4072;
  type_item.typname = "jsonpath";
  type_item.typreceive = "jsonpath_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 4073;
  type_item.typname = "_jsonpath";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4072;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2970;
  type_item.typname = "txid_snapshot";
  type_item.typreceive = "txid_snapshot_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 2949;
  type_item.typname = "_txid_snapshot";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2970;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 5038;
  type_item.typname = "pg_snapshot";
  type_item.typreceive = "pg_snapshot_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 5039;
  type_item.typname = "_pg_snapshot";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 5038;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4451;
  type_item.typname = "int4multirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6150;
  type_item.typname = "_int4multirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4451;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4532;
  type_item.typname = "nummultirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6151;
  type_item.typname = "_nummultirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4532;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4533;
  type_item.typname = "tsmultirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6152;
  type_item.typname = "_tsmultirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4533;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4534;
  type_item.typname = "tstzmultirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6153;
  type_item.typname = "_tstzmultirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4534;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4535;
  type_item.typname = "datemultirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6155;
  type_item.typname = "_datemultirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4535;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4536;
  type_item.typname = "int8multirange";
  type_item.typreceive = "multirange_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6157;
  type_item.typname = "_int8multirange";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 4536;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2275;
  type_item.typname = "cstring";
  type_item.typreceive = "cstring_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 1263;
  type_item.typname = "_cstring";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 2275;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2277;
  type_item.typname = "anyarray";
  type_item.typreceive = "anyarray_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 2278;
  type_item.typname = "void";
  type_item.typreceive = "void_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 5078;
  type_item.typname = "anycompatiblearray";
  type_item.typreceive = "anycompatiblearray_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4600;
  type_item.typname = "pg_brin_bloom_summary";
  type_item.typreceive = "brin_bloom_summary_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 4601;
  type_item.typname = "pg_brin_minmax_multi_summary";
  type_item.typreceive = "brin_minmax_multi_summary_recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  type_item.oid = 6437;
  type_item.typname = "oid8";
  type_item.typreceive = "oid8recv";
  type_item.child_oid = 0;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }
  type_item.oid = 6442;
  type_item.typname = "_oid8";
  type_item.typreceive = "array_recv";
  type_item.child_oid = 6437;
  rc = resolver.Insert(type_item, error);
  if (rc != 0) {
    return rc;
  }

  return rc;
}

}  // namespace adbcpq
